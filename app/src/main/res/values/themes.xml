<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MExpense" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/bgColor</item>
        <item name="textInputStyle">@style/TextInputLayoutStyle</item>
        <item name="actionBarTheme">@style/MyActionBar</item>

    </style>

    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/editTextBorder</item>
        <item name="boxStrokeWidth">1dp</item>
<!--        <item name="hintEnabled">false</item>-->
<!--        <item name="labelStyle">@null</item>-->
        <item name="errorEnabled">true</item>
        <item name="boxBackgroundColor">@null</item>
        <item name="labelStyle">@null</item>
    </style>

    <!--  Action Bar Menu Styles  -->

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>